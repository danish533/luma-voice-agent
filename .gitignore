.venv/
.venv-*/
__pycache__/
*.py[cod]
.env
logs/
.pytest_cache/
# eval/results/ is deliberately tracked: the raw scenario output is part of the
# submission, not a build artifact.

# Vendored browser SDK (re-fetch with scripts/fetch_vendor.sh)
ops/vendor/
